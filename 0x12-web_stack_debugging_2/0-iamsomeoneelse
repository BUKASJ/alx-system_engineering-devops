#!/usr/bin/env bash
# This script runs the whoami command under the user passed as an argument

if [ "$#" -ne 1 ]; then
  echo "Usage: $0 <username>"
  exit 1
fi

USER=$1

# Use su to switch to the user and run whoami
su -c "whoami" "$USER"
